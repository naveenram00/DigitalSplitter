import java.io.File;
import java.io.IOException;


public class test {
	/* public static void main(String [] args) throws IOException {
		
		test t = new test();
		 t.ListAllFiles("/Users/naveen/Desktop/Desktop");
	}
	public void ListAllFiles(String directory) throws IOException {
	File f = new File(directory); // current directory

    File[] files = f.listFiles();
    for (File file : files) {
        System.out.println(file.getCanonicalPath());
        }
   
		File f = new File("/Users/naveen/Desktop/Desktop/Windows"); // current directory

	    File[] files = f.listFiles();
	    for (File file : files) {
	    	if (file.contains(".")) {
	    		AllSongs.push(file);
	    		System.out.println("Directory: " + file.getCanonicalPath());
	    	} else {
	    		AllDirectories.push(file);
	    		System.out.println(file.getCanonicalPath());
	}
    
*/
}
